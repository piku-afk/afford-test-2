import { MenuItem, styled } from '@mui/material';

export const SearchMenuItem = styled(MenuItem)({
  fontSize: 14,
  paddingTop: 6,
  paddingBottom: 6,
});

export const CategoryRowMenuItem = styled(MenuItem)({
  fontSize: 13,
  paddingTop: 4,
  paddingBottom: 4,
});
